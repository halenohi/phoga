%table.table
  %thead
    %tr
      %th 名称
      %th &nbsp;
  %tbody
    - @tags.each do |tag|
      %tr
        %td= tag.name
        %td
          = link_to '編集', edit_tag_path(tag), { class: 'btn btn-default' }

.well
  = link_to '新規作成', new_tag_path, { class: 'btn btn-default' }
